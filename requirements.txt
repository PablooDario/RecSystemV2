# Movie Recommender System — Python dependencies
# Install: python3.12 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# Core scientific stack
numpy>=1.24,<2.0
pandas>=2.0
pyarrow>=14.0               # for reading .parquet files

# ML / recommendation models
scikit-surprise>=1.1.3       # SVD, SVD++ (Cython-based CF)
implicit>=0.7                # BPR-MF (Cython multi-threaded)
scipy>=1.10                  # sparse matrices for implicit

# Backend (FastAPI)
fastapi>=0.100
uvicorn>=0.22
sqlalchemy>=2.0
pymysql>=1.1
python-dotenv>=1.0
pydantic>=2.0
passlib[bcrypt]>=1.7
bcrypt==4.0.1
cryptography>=41.0
requests==2.34.2

# Visualization & display (used in notebooks)
matplotlib>=3.7
tabulate>=0.9

# Testing
pytest>=7.0
hypothesis>=6.0

# Notebook support (only needed to run .ipynb files)
ipykernel>=6.0
ipython>=8.0
ipywidgets>=8.0              # for tqdm.notebook progress bars
tqdm>=4.0                    # progress bars in grid search cells
